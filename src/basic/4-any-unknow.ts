// NOTE: Disable "strict" rule in tsconfig.json to avoid error.

// NOTE: "any" type is the most flexible type in TypeScript.

function onFixed(num: number) {
  return num.toFixed(2)
}

let num = 1234.111
