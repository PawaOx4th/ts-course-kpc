/**
 * Unit : Type Guards.
 *  - typeof.
 *  - instanceof.
 *  - in.
 */

// NOTE: Type Guards.
// Type Guards are a way to tell the compiler that the type of some entity is
// a specific type so that the compiler can narrow down the type of other entities.
// ------------------------------------------------------------
//  - Type Guards are used to narrow down the type of an
//    - variable.
//    - parameter.
//    - property.
//    - function.
//    - class.
//    - generic type.
//    - type alias.
//    - type parameter.
//    - discriminated union.

// Example 1: Type Guards with `typeof`.

// Example 2: Type Guards with `instanceof`.

// Example 3: Type Guards with `in`.

// Example 4: Type Guards with `discriminated union`.

// Example 5: Type Guards with user defined type guards with `is`.

// Custom Type Guards. truncateString()
